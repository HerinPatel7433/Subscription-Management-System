feat: build full Dashboard screen with live data, charts, and activity feed

- Replace stub DashboardPage with full implementation
- 4 KPI summary cards: Active Subscriptions, Monthly Revenue (with % change
  vs previous month), Pending Invoices (clickable), Overdue Invoices (clickable)
- Skeleton loaders for each card section while data is fetching
- Per-section error boundaries: one failing API shows amber warning banner
  and falls back to mock data without breaking other sections
- Revenue bar chart (Recharts BarChart, last 6 months, highlights peak month)
- Subscription status donut/pie chart (Active/Draft/Confirmed/Quotation/Closed)
- Dual recent-activity tables: last 5 subscriptions + last 5 invoices,
  each row links to the detail page
- Quick action buttons (admin only): New Subscription, New Product,
  Generate Invoices (modal confirmation before triggering billing job)
- Time-based greeting with user first name
- Role-aware: admin sees all buttons, portal sees summary only
- Add dashboard API methods to billingService:
  getDashboardActivity, getSubscriptionStatusBreakdown, triggerBillingJob
- All sections fall back gracefully to realistic mock data
